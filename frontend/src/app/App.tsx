/**
 * The application root (`INF-3a`).
 *
 * Deliberately one line of text. `INF-1`'s criterion for the frontend was that it "starts up
 * empty, with its own hello", and this is that hello -- kept until `UI-4a` puts the router here
 * and `UI-4c` puts the shell inside it. It is styled with nothing, because a colour written here
 * would be the first value in the repository that is not a token.
 */

export function App() {
  return <main>Sonarium</main>;
}
