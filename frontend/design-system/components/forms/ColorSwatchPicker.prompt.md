Colour picker for the create- and edit-library dialogs. Seven curated hues, never a free colour picker.

```jsx
<ColorSwatchPicker value="clay" onChange={setColour} />
```

The chosen colour appears as the card chip and the sidebar dot. It identifies a library and never carries meaning on its own.
