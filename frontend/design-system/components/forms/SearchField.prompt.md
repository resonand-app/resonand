The one search field in the product: centred in the top nav, max-width 540px. Typing opens `SearchResults` beneath it; Enter goes to the full search view.

```jsx
<SearchField style={{ maxWidth: 540 }} />
<SearchField value="field recording" focused />
```
