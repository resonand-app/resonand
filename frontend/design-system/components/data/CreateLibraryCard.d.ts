import * as React from "react";

/**
 * The first tile in the library grid: same footprint as a library card, no elevation.
 * @startingPoint section="Data" subtitle="Create-library tile" viewport="700x230"
 */
export interface CreateLibraryCardProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {}

export declare function CreateLibraryCard(props: CreateLibraryCardProps): React.JSX.Element;
