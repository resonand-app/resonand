Always the first tile in the library grid, at exactly the same size as a library card so the grid stays even.

```jsx
<CreateLibraryCard onClick={openCreateDialog} />
```

It is a recessed fill rather than a raised card — it is an invitation, not an object. It is also the empty state: a new account sees this tile alone.
