# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# Node
node_modules/
.vite/
frontend/dist/
frontend/coverage/

# Local data
*.db
*.db-wal
*.db-shm
storage/
.env
.env.local

# Editors and system
.DS_Store
.idea/
*.swp

# Internal working docs — deliberately never committed.
# The functional specification, the interface specification, the strategy notes and the design
# provenance stay local. The public repo carries README.md, VISION.md, ROADMAP.md,
# docs/v0-plan.md, docs/ui-plan.md and frontend/design-system/, which the application consumes
# directly.
docs/internal/

# Repo snapshots and archives — never committed
*.zip
*.tar.gz
